@page "/docs/extensions-whats-next"
@attribute [Microsoft.AspNetCore.Authorization.Authorize]
@using ALDevToolbox.Components.Shared

@*
    Landing page users are sent to after Generate finishes on
    /templates/workspace or /templates/extension. The ZIP download is already
    triggered by the time this page loads (the form's response carried
    Content-Disposition: attachment); this page picks up the user where
    the browser's download dialog left them.

    Page archetype 12 (docs / long-form): `.page-head` + `.docs` with a `.prose`
    column and an on-page `.docs__toc`. The TOC is new here - the page is five
    sections of reading and the reader arrives mid-task, wanting the git bit,
    not the intro.

    Tone is intentionally soft - many of our users are Business Central
    consultants who do some development but aren't full-time git
    practitioners. Avoid imperative "you must" language; explain the
    why before the what.

    Provider-agnostic by design: we name the popular hosts in passing
    but don't favour one. The generic `git remote add origin <url>`
    pattern works the same way on GitHub, Azure DevOps, GitLab,
    Bitbucket, and self-hosted Gitea.
*@

<PageTitle>What's next · AL Dev Toolbox</PageTitle>

<div class="page">
    <header class="page-head">
        <div>
            <h1 class="page-head__title">You're all set - here's what's next</h1>
            <p class="page-head__sub">
                Opening what you just downloaded, and putting it somewhere safe.
            </p>
        </div>
    </header>

    <div class="docs">
        <div class="docs__main">
            <article class="prose">
                <p class="prose__lead">
                    Your workspace or extension is in your Downloads folder. The rest of
                    this page walks through opening it, putting it under source control,
                    and pushing it to wherever your team keeps its code. Nothing here is
                    mandatory - if you already have a git workflow you like, skip ahead.
                </p>

                <h2 id="wn-open">1. Open the folder</h2>
                <p>
                    Unzip the file you just downloaded somewhere you'll find it again -
                    most people pick a <code>Projects</code> or <code>Source</code>
                    folder under their user directory. The unzipped folder is your
                    workspace; everything inside it belongs together.
                </p>
                <p>
                    Open that folder in VS Code (<em>File &rarr; Open Folder...</em>) and,
                    if it asks, trust the workspace. If there's more than one extension
                    folder inside, open the <code>.code-workspace</code> file instead
                    (<em>File &rarr; Open Workspace from File...</em>) - that gets you all
                    the folders in one window. If VS Code prompts you to install the
                    <strong>AL Language</strong> extension, accept - that's the Microsoft
                    extension that gives you IntelliSense, compile, and launch for
                    Business Central projects.
                </p>
                <div class="note note--info">
                    <span class="note__icon"><Icon Name="info" Width="17" Height="17" /></span>
                    <span class="note__title">One file to leave alone</span>
                    <span class="note__body">
                        A <code>workspace.aldt.toml</code> file sits at the root. You don't
                        need to open it - it travels with the project and records how the
                        workspace was set up.
                    </span>
                </div>

                <h2 id="wn-git">2. Save it with git</h2>
                <p>
                    Git is the safety net that records every change you make so you
                    can roll back when something breaks, see who changed what, and
                    share your work with teammates. The workspace already has a
                    <code>.gitignore</code> in it, so git knows to skip the build
                    output and other generated files.
                </p>
                <p>
                    Open a terminal inside the workspace folder (in VS Code:
                    <em>Terminal &rarr; New Terminal</em>) and run these three
                    commands. They tell git to start watching the folder, mark every
                    file to be included, and record the first snapshot.
                </p>
                <div class="note note--info">
                    <span class="note__icon"><Icon Name="info" Width="17" Height="17" /></span>
                    <span class="note__title">First time on this machine?</span>
                    <span class="note__body">
                        Git is a separate program you install once. If your terminal says
                        <code>git</code> isn't recognised, get it from git-scm.com, accept the
                        default options, then close and reopen the terminal and try again.
                    </span>
                </div>
                <div class="codeblock">
                    <div class="codeblock__bar">
                        <span class="codeblock__lang">shell</span>
                        <span class="codeblock__spacer"></span>
                        <button type="button" class="btn btn--sm copy-btn" data-copy-target="#wn-git-init">
                            <Icon Name="copy" Width="14" Height="14" />
                            <span data-copy-label>Copy</span>
                        </button>
                    </div>
                    <pre class="codeblock__pre" id="wn-git-init">git init
git add .
git commit -m "Initial workspace from AL Dev Toolbox"</pre>
                </div>
                <p class="prose__cap">
                    If git complains that it doesn't know who you are, run these two once
                    per machine and then re-run <code>git commit</code>. Git stamps your
                    name on every change you record.
                </p>
                <div class="codeblock">
                    <div class="codeblock__bar">
                        <span class="codeblock__lang">shell</span>
                        <span class="codeblock__spacer"></span>
                        <button type="button" class="btn btn--sm copy-btn" data-copy-target="#wn-git-config">
                            <Icon Name="copy" Width="14" Height="14" />
                            <span data-copy-label>Copy</span>
                        </button>
                    </div>
                    <pre class="codeblock__pre" id="wn-git-config">git config --global user.name "Kirsten Jensen"
git config --global user.email "kirsten.jensen@cronus.example"</pre>
                </div>

                <h2 id="wn-push">3. Put it on your team's git server</h2>
                <p>
                    "Push" means uploading your local history to a server so other
                    people can see it and so your work survives if your laptop
                    disappears. The server is usually one of:
                </p>
                <ul>
                    <li><strong>GitHub</strong> - common for open source and many product teams.</li>
                    <li><strong>Azure DevOps</strong> - common for Microsoft-stack consultancies and enterprises.</li>
                    <li><strong>GitLab</strong>, <strong>Bitbucket</strong>, <strong>Gitea</strong>, or your own self-hosted server - the underlying git is the same.</li>
                </ul>
                <p>
                    Each provider has its own UI for creating a new repository, but the
                    steps are roughly the same: sign in, click <em>New repository</em>
                    (or <em>New repo</em>), give it a name, and leave the "Add a README"
                    and "initialize with" boxes <strong>unchecked</strong>. If the new
                    repository already has a file in it, your first push is rejected and
                    untangling it takes commands that aren't on this page.
                </p>
                <p>
                    Once it's created, the provider shows you a URL that looks
                    something like one of these:
                </p>
                <div class="codeblock">
                    <div class="codeblock__bar">
                        <span class="codeblock__lang">example</span>
                    </div>
                    <pre class="codeblock__pre">https://github.com/your-org/customer-cronus.git
https://dev.azure.com/your-org/your-project/_git/customer-cronus
https://gitlab.com/your-org/customer-cronus.git</pre>
                </div>
                <p class="prose__cap">
                    Providers usually offer two forms of the same address. Pick the one
                    starting <code>https://</code> - the <code>git@@</code> form needs an SSH
                    key set up first, which is a separate job.
                </p>
                <p>
                    Copy whichever URL your provider shows. Back in your terminal,
                    point your local repository at it and push the first commit:
                </p>
                <div class="codeblock">
                    <div class="codeblock__bar">
                        <span class="codeblock__lang">shell</span>
                        <span class="codeblock__spacer"></span>
                        <button type="button" class="btn btn--sm copy-btn" data-copy-target="#wn-git-push">
                            <Icon Name="copy" Width="14" Height="14" />
                            <span data-copy-label>Copy</span>
                        </button>
                    </div>
                    <pre class="codeblock__pre" id="wn-git-push">git remote add origin &lt;paste the URL here&gt;
git branch -M main
git push -u origin main</pre>
                </div>
                <div class="note note--info">
                    <span class="note__icon"><Icon Name="info" Width="17" Height="17" /></span>
                    <span class="note__title">The first push will ask who you are</span>
                    <span class="note__body">
                        That's expected, not a failure. Usually a browser window opens -
                        sign in to your provider there and it remembers you afterwards. If
                        you're asked for a <em>password</em> and your normal one is
                        rejected, your provider wants an access token instead; whoever
                        administers your git server can issue one.
                    </span>
                </div>
                <p class="prose__cap">
                    That second line renames your local branch to <code>main</code>
                    so it lines up with what most hosts expect by default. If you'd
                    rather use <code>master</code>, <code>trunk</code>, or anything
                    else, swap it in - git doesn't care. The <code>-u</code> in the
                    push command tells git "remember this server next time", so from
                    then on a plain <code>git push</code> works.
                </p>
                <p>
                    Refresh your provider's page in the browser - your files should be
                    listed there. That's it; your work is now in two places.
                </p>

                <h2 id="wn-daily">From here on</h2>
                <p>
                    Day-to-day, the loop is small: edit files, save, then run
                    <code>git add</code> / <code>git commit</code> / <code>git push</code>
                    when you've finished something worth saving. VS Code's
                    <strong>Source Control</strong> pane (the branch icon in the
                    sidebar) gives you buttons for all of that if you'd rather not
                    type the commands.
                </p>
                <p>
                    If you need another extension on top of this workspace later,
                    head back to <a href="/templates/extension">New extension</a> -
                    pick the same template and AL Dev Toolbox will add a sibling
                    folder alongside the ones you already have.
                </p>

                <h2 id="wn-stuck">Stuck?</h2>
                <div class="note note--tip">
                    <span class="note__icon"><Icon Name="lightbulb" Width="17" Height="17" /></span>
                    <span class="note__title">Nothing here can lose your work</span>
                    <span class="note__body">
                        None of the commands above will destroy work you haven't pushed
                        yet. If something looks wrong, take a breath; the original ZIP
                        is still in your Downloads folder, and a fresh
                        <a href="/templates/workspace">New workspace</a> is always one
                        click away.
                    </span>
                </div>
                <p>
                    For team-specific things - which server to use, what the repository
                    should be named, who owns what - your tech lead or architect is the
                    right person to ask.
                </p>
                <p class="wn-acts">
                    <a class="btn" href="/">
                        <Icon Name="house" Width="16" Height="16" />
                        <span>Go to Home</span>
                    </a>
                    <a class="btn" href="/templates/extension">
                        <Icon Name="file-plus" Width="16" Height="16" />
                        <span>Create another extension</span>
                    </a>
                </p>
            </article>
        </div>

        <nav class="docs__toc" aria-label="On this page">
            <span class="docs__toc-h">On this page</span>
            <div class="docs__toc-list">
                <a class="toc-link" href="#wn-open">1. Open the folder</a>
                <a class="toc-link" href="#wn-git">2. Save it with git</a>
                <a class="toc-link" href="#wn-push">3. Put it on your git server</a>
                <a class="toc-link" href="#wn-daily">From here on</a>
                <a class="toc-link" href="#wn-stuck">Stuck?</a>
            </div>
        </nav>
    </div>
</div>
