@*
    Anchor component for the shared CodeMirror JS module
    (CodeEditor.razor.js). Blazor's `.razor.js` JS-isolation convention
    requires a matching `.razor` component with the same base name; this
    file exists to satisfy that pairing.

    Callers don't render <CodeEditor /> — they import the companion module
    directly via IJSRuntime (see AdminTemplateEdit and
    AdminConfigurationWorkspace). Rendering this component does nothing.
*@
@code { }
