# Business Central 2023 Release Wave 1 (v22). Pairs the 22.0.0.0 application
# version with the matching dotted runtime BC v22 ships with. Keep the
# `key` stable — it's the identifier templates store in
# `default_application_version`.

[version]
key = "bc-v22"
name = "Business Central 2023 Release Wave 1 (v22)"
application = "22.0.0.0"
runtime = "11.0"
ordering = 100
